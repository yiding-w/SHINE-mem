# MemoryTest Prepare Data Utilities

```bash
python -m MemoryTest.prepare_data.prepare_memory_data
python -m MemoryTest.prepare_data.generate_capacity_data
```

`prepare_memory_data` creates person-disjoint semantic fact splits. `generate_capacity_data` is the legacy capacity/distractor data generator moved out of the `MemoryTest` root.
